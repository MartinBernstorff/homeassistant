  movie_mode:
    sequence:
      - service: input_select.select_option
        data:
          entity_id: input_select.context
          option: Movie-mode
  normal:
    sequence:
      - service: input_select.select_option
        data:
          entity_id: input_select.context
          option: Normal
  cozy:
    sequence:
      - service: input_select.select_option
        data:
          entity_id: input_select.context
          option: Cozy
  pre_sleep:
    sequence:
      - service: input_select.select_option
        data:
          entity_id: input_select.context
          option: Pre-sleep
  sleep:
    sequence:
      - service: input_select.select_option
        data:
          entity_id: input_select.context
          option: Sleep
  away:
    sequence:
      - service: input_select.select_option
        data:
          entity_id: input_select.context
          option: Away
